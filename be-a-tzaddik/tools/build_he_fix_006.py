#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Build he_fix_006_he_only.json."""

from __future__ import annotations

import json
import re
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1] / "data" / "translation-catalog"
HUMAN = ROOT / "human"
MIXED_RE = re.compile(r"[\u0590-\u05FF]+[A-Za-z]+|[A-Za-z]+[\u0590-\u05FF]+")

he_fix_006 = [
    "לוי הוא השבט המשרת במקדש; היום לויים נקראים לתורה אחרי כהנים. רבים נושאים שם משפחה לוי או מסורות קשורות. לכהנים ולויים יש הלכות נוספות (ברכת כהנים, טומאה). אם אינכם בטוחים במעמדכם — שאלו משפחה ואת הרב.",
    (
        "מעריב הוא תפילת ערב, שהוסדרה על ידי אבינו יעקב ונתפללת בכל לילה.\n\n"
        "מה זה:\n"
        "מעריב (מַעֲרִיב — נקרא גם ערבית) הוא שירות התפילה של הערב. אחרי קריאת שמע וברכותיה, "
        "אומרים את העמידה (שmona esrei) — אותה תפילה עומדת כמו בשacharit ובmincha, "
        "אם כי גרסת הערב שונה במקצת.\n\n"
        "מבנה מעריב:\n"
        "1. אשרי וקטע קצר (במנהגים מסוימים)\n"
        "2. ברכו — קריאה לתפילה (בָּרְכוּ — «ברכו את ה'»), נאמרת בתפילה עם מניין של 10 גברים\n"
        "3. קריאת שמע של ערב עם שתי ברכות לפניה ושתיים אחריה\n"
        "4. עמידה / שmona esrei\n"
        "5. עלינו\n"
        "6. קדיש יatom (נאמר על ידי אבלים או מי שמציין יahrzeit)\n\n"
        "זמנים:\n"
        "מצאת הכוכבים ועד עלות השחר. אם פספסתם מעריב — משלימים בשacharit של הבוקר הבא בלבד: "
        "תחילה העמידה הרגילה של שacharit, ואז מיד עמידה שנייה כתשלומin למעריב. "
        "אם פספסתם גם את החלון הזה — ההזדמנות אבדה לצמיתות. "
        "ראו את הערך על תשלומin לכללים מלאים, כולל אפשרות תפilat nedavah."
    ),
]

if __name__ == "__main__":
    bad = [(i + 1, m.group()) for i, s in enumerate(he_fix_006) for m in MIXED_RE.finditer(s)]
    print("bad", bad)
    print(len(he_fix_006))
