import Foundation

enum Secrets {
    // Replace with your actual GitHub token
    static let githubToken = "YOUR_GITHUB_TOKEN"
    /// Optional — enables online translation for 60+ languages (same as Android `GOOGLE_TRANSLATE_API_KEY`).
    static let googleTranslateApiKey = "YOUR_GOOGLE_TRANSLATE_API_KEY_HERE"
} 