import Foundation

/// Mitz Mode is English-only. Device-language bootstrap is disabled.
/// Archived implementation: `archived-translations/DeviceLanguageBootstrap.swift`
enum DeviceLanguageBootstrap {
    static func applyIfNeeded() {}
}
