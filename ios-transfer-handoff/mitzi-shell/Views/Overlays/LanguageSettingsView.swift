import SwiftUI

/// Archived — Mitz Mode is English-only. See `archived-translations/LanguageSettingsView.swift`.
struct LanguageSettingsView: View {
    var body: some View {
        EmptyView()
    }
}
