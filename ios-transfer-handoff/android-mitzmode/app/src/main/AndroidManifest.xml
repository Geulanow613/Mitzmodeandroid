<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BROADCAST_CLOSE_SYSTEM_DIALOGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <application
        android:name=".MitzModeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MitzMode"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        android:largeHeap="true"
        android:localeConfig="@xml/locales">
        
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />
        <!-- DSN is set programmatically in MitzModeApplication only when valid.
             Leaving it out of the manifest prevents SentryInitProvider from
             crashing at ContentProvider attach time on devices without a DSN
             configured (e.g. open-source builds). -->
        <!-- Sample rates are set in MitzModeApplication.initializeSentry() to avoid manifest type mismatches. -->
        <meta-data
            android:name="io.sentry.debug"
            android:value="true" />

        <!-- Disable ProfileInstaller (avoids ART trace buffer conflicts on emulators). -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MitzMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MusicPlayerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver 
            android:name=".service.MusicNotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.beardytop.mitzmode.PLAY_PAUSE" />
                <action android:name="com.beardytop.mitzmode.STOP" />
                <action android:name="com.beardytop.mitzmode.CLOSE" />
            </intent-filter>
        </receiver>

    </application>

    <queries>
        <package android:name="com.miui.powerkeeper" />
    </queries>
</manifest>