pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}
rootProject.name = "MitzMode"
include ':app'
include ':baselineprofile'
include ':beatzaddik-shared'
// One source of truth — Geulanow613/sharedmodule (cloned as hehehe/sharedmodule)
project(':beatzaddik-shared').projectDir = file('sharedmodule/shared')
