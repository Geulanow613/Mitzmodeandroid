# Unused store media (not shipped)

These files are kept for possible re-enable later but are **not** packaged into
App Store / Play Store builds:

- mitzmodenew1–13.mp4 (milestone reward videos; feature flag off)
- background.mp4 (legacy looping home background)
- tzaddik.mp4 (legacy blessed animation)
- song.mp3 (Official App Song asset)

**Kept in the app:** `finalreward.mp4`

Android: live under `app/src/debug/assets` (debug builds only).
iOS: live under `UnusedMedia/` (excluded from Xcode / xcodegen).
